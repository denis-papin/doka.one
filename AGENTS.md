# AGENTS.md

## Database Connection

Use the local PostgreSQL database with the following settings:

- JDBC URL: `jdbc:postgresql://localhost:5432/postgres`
- Username: `doka`
- Password: read from the `PG_PASSWORD` environment variable

## Documentation

Project documentation is available at `~/Dropbox/ISD Tech/Org/Wiki/Doka documents`.
